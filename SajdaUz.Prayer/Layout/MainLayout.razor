@using NamozVaqtlari.Themes
@inherits LayoutComponentBase

<MudThemeProvider @bind-IsDarkMode="@isDarkMode" Theme="Theme.SpacesTheme()" />

<MudAppBar Color="Color.Primary" Fixed="false">
    <MudText Typo="Typo.h6">
        Namoz Vaqtlari
    </MudText>

    <MudSpacer />
    <MudToggleIconButton @bind-Toggled="@isDarkMode" Icon="@Icons.Material.Filled.LightMode" Color="Color.Inherit"
                         ToggledIcon="@Icons.Material.Filled.DarkMode" ToggledColor="Color.Inherit" />
    <MudIconButton Icon="@Icons.Custom.Brands.GitHub" Color="Color.Inherit" />
</MudAppBar>

<!-- Adjust MudContainer to respect the space taken by MudAppBar -->
<MudContainer Style="margin-top: 100px;">  <!-- Assuming the height of the MudAppBar is 64px -->
    @Body
</MudContainer>

@code {
    private bool isDarkMode = true;
}